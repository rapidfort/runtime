# runtime
RapidFort runtime releases

## usage
./rf-cmd-darwin-arm64 -h us01.rapidfort.com -u <username> -p <password> -r quay.io/rapidfort -c cri -ru <registry-username> -rp <registry-password> --rev 1.0.20
